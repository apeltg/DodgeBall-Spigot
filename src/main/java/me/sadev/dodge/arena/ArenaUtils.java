package me.sadev.dodge.arena;

public class ArenaUtils {

    /*

    // TODO - Quando terminar o arena.getAll()

    public Arena getPlayerArena(Player player) {
        arena.getAll().forEach(arena -> {
            if new IntRange(arena.pos1.getX(), arena.pos2.getX()).containsNumber(player.getLocation().getX())
                && IntRange(arena.pos1.getY(), arena.pos2.getY()).containsNumber(player.getLocation().getY())
                && IntRange(arena.pos1.getZ(), arena.pos2.getZ()).containsNumber(player.getLocation().getZ());
            return arena;
        })
        return null;
    }
     */
}
